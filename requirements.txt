# Dépendances d'exécution (versions figées). Installer : pip install -r requirements.txt
Django==6.0.2
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-environ==0.14.0
mysqlclient==2.2.8
pillow==12.3.0
# Serveur WSGI de production (Linux uniquement)
gunicorn>=23; sys_platform != "win32"
